# api/ia_api/alertmanager/alertmanager.yml

global:
  resolve_timeout: 5m

route:
  group_by: ['alertname', 'severity']
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 1h
  receiver: 'null' # Pour la démo, on n'envoie les alertes nulle part

receivers:
- name: 'null'